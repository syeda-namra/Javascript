let userName=`\n                    Syeda Namra Ali          `;

console.log(`With Whitspaces: `,userName);

console.log(`Without Whitespaces: `,userName.trim());

//console.log(userName.trimStart());  //remove whitespaces from the start of the string without changing the original string

//console.log(userName.trimEnd());  //remove whitespaces from the last of the string without changing the original string